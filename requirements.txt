# ============================================================
# FYP: Deep Learning for Jet Classification in pp Collisions
# BNBWU-CERN Collaboration
# ============================================================
# Install: pip install -r requirements.txt
# For GPU support, install PyTorch with CUDA first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# Core
# Pin numpy<2 because stock PyTorch/XGBoost wheels on macOS/Linux (torch<2.3,
# xgboost<2.1) are compiled against NumPy 1.x ABI; NumPy 2.x triggers segfaults
# and "_ARRAY_API not found" warnings. Upgrade together if you move past this.
numpy>=1.24,<2
pandas>=2.0
scipy>=1.10
h5py>=3.8
tables>=3.8
scikit-learn>=1.3

# Deep Learning
torch>=2.0
torchvision>=0.15

# HEP-specific
energyflow>=1.3
vector>=1.1

# Visualization
matplotlib>=3.7
seaborn>=0.13
mplhep>=0.3

# Experiment tracking
tqdm>=4.65

# Boosted Decision Trees (baseline comparison)
xgboost>=2.0
lightgbm>=4.0

# Jupyter
jupyter>=1.0
ipywidgets>=8.0
